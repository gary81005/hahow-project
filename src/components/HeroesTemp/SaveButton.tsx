import { LoadingButton, LoadingButtonProps } from '@mui/lab';

const SaveButton = (props: LoadingButtonProps) => {
  return <LoadingButton {...props}>儲存</LoadingButton>;
};

export default SaveButton;
